Install_Pinnacle.bat - is the batch file that installs all the pre-requisites and the *.msi file associated with the project.

Install_Pinnacle.vbs - is the script that is needed to push the application on the KBox.

PinnacleSetup directory contains the C# code that creates the *.msi file.
